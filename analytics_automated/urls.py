from django.conf.urls import include, url
from django.contrib import admin

urlpatterns = [
    # url(r'^submission/$', SubmissionList.as_view(), name='submission_list'),
]

# from django.urls import path
# from .views import VisualizationDashboardView, CWLUploadPageView

# urlpatterns = [
#     path('upload/', CWLUploadPageView.as_view(), name='upload'),
#     path('dashboard/', VisualizationDashboardView.as_view(), name='dashboard'),
# ]

